<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>섹션 1 - 히어로 영역</title>
    
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        
        body {
            font-family: 'Pretendard', -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;
            line-height: 1.6;
            color: #333;
        }
        
        /* ========== 섹션 1: 히어로 타이틀 ========== */
        .hero-section {
            position: relative;
            height: 100vh;
            min-height: 600px;
            display: flex;
            align-items: center;
            justify-content: center;
            overflow: hidden;
        }
        
        .hero-content {
            position: relative;
            z-index: 3;
            text-align: center;
            color: white;
            animation: fadeInUp 1.5s ease-out;
            background: rgba(0, 0, 0, 0.1); /* 90% 투명 (10% 불투명) */
            padding: 25px 60px;  /* 60px → 25px (위아래), 80px → 60px (좌우) */
            border-radius: 20px;
            backdrop-filter: blur(10px);
            max-width: 900px;
        }
        
        @media (max-width: 768px) {
            .hero-content {
                padding: 20px 25px;  /* 40px → 20px (위아래), 30px → 25px (좌우) */
                margin: 0 20px;
            }
        }
        
        .hero-title {
            font-size: clamp(2rem, 5vw, 3.5rem);
            font-weight: 800;
            margin-bottom: 1rem;  /* 1.5rem → 1rem */
            line-height: 1.2;
            text-shadow: 2px 2px 4px rgba(0,0,0,0.8);
        }
        
        /* 제목 줄바꿈 최적화 */
        @media (max-width: 768px) {
            .hero-title {
                font-size: 1.6rem;  /* 크기 조정으로 2줄 유지 */
                line-height: 1.3;
            }
        }
        
        @media (max-width: 480px) {
            .hero-title {
                font-size: 1.4rem;  /* 작은 모바일에서도 2줄 유지 */
                line-height: 1.35;
            }
        }
        
        @media (max-width: 360px) {
            .hero-title {
                font-size: 1.25rem;  /* 초소형 모바일 대응 */
                line-height: 1.4;
            }
        }
        
        .hero-subtitle {
            font-size: clamp(1.2rem, 3vw, 2.2rem);  /* PC 크기 증가 */
            font-weight: 400;
            color: #FFD700;
            text-shadow: 1px 1px 3px rgba(0,0,0,0.8);
            margin-bottom: 2rem;  /* 3rem → 2rem */
            max-width: 700px;
            margin-left: auto;
            margin-right: auto;
            line-height: 1.3;  /* 줄간격 축소 */
        }
        
        /* 서브타이틀 반응형 줄바꿈 */
        .hero-subtitle .line1,
        .hero-subtitle .line2,
        .hero-subtitle .line3 {
            display: block;
            margin-bottom: 0.1rem;  /* 줄간격 더 좁게 */
        }
        
        @media (max-width: 768px) {
            .hero-subtitle {
                font-size: 1.1rem;
                line-height: 1.4;
                padding: 0 10px;
            }
            
            .hero-subtitle .line1,
            .hero-subtitle .line2,
            .hero-subtitle .line3 {
                display: block;
                margin-bottom: 0.2rem;
            }
        }
        
        @media (max-width: 480px) {
            .hero-subtitle {
                font-size: 1rem;
            }
            
            .hero-subtitle .line1,
            .hero-subtitle .line2,
            .hero-subtitle .line3 {
                margin-bottom: 0.15rem;
            }
        }
        
        .hero-highlight {
            display: inline-block;
            background: linear-gradient(135deg, #FF6B35 0%, #F7931E 100%);
            padding: 0.8rem 2rem;  /* 패딩 증가 */
            border-radius: 50px;
            font-size: 1.3rem;  /* 폰트 크기 증가 */
            font-weight: 600;
            margin-top: 0.8rem;  /* 1.5rem → 0.8rem */
            animation: pulse 2s infinite;
        }
        
        @media (max-width: 768px) {
            .hero-highlight {
                font-size: 1.1rem;
                padding: 0.6rem 1.5rem;
            }
        }
        
        @media (max-width: 480px) {
            .hero-highlight {
                font-size: 1rem;
                padding: 0.5rem 1.2rem;
            }
        }
        
        .scroll-indicator {
            position: absolute;
            bottom: 30px;
            left: 50%;
            transform: translateX(-50%);
            z-index: 3;
            animation: bounce 2s infinite;
            cursor: pointer;
        }
        
        .scroll-indicator::before {
            content: '⌄';
            color: white;
            font-size: 2rem;
            opacity: 0.7;
        }
        
        /* 애니메이션 */
        @keyframes fadeInUp {
            from {
                opacity: 0;
                transform: translateY(30px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }
        
        @keyframes bounce {
            0%, 20%, 50%, 80%, 100% {
                transform: translateX(-50%) translateY(0);
            }
            40% {
                transform: translateX(-50%) translateY(-10px);
            }
            60% {
                transform: translateX(-50%) translateY(-5px);
            }
        }
        
        @keyframes pulse {
            0% {
                box-shadow: 0 0 0 0 rgba(255, 215, 0, 0.7);
            }
            70% {
                box-shadow: 0 0 0 20px rgba(255, 215, 0, 0);
            }
            100% {
                box-shadow: 0 0 0 0 rgba(255, 215, 0, 0);
            }
        }
    </style>
</head>
<body>
    <!-- 섹션 1: 히어로 타이틀 영역 -->
    <section id="s20250820d39b4d14b3e2d" class="hero-section">
        <div class="hero-content">
            <h1 class="hero-title">
                기업 워크샵의 새로운 기준<br>
                모든 것이 준비된 올인클루전
            </h1>
            <p class="hero-subtitle">
                <span class="line1">숙박·조식·바베큐·주류까지</span>
                <span class="line2">무한리필로 제공되는</span>
                <span class="line3">단체 워크샵 전용 펜션</span>
            </p>
            <span class="hero-highlight">기업 단체 야유회 전문 × 6시간 훈연 바베큐</span>
        </div>
        
        <div class="scroll-indicator"></div>
    </section>
</body>
</html>